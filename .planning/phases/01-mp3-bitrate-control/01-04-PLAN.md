---
phase: 01-mp3-bitrate-control
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - server.sh
  - tools/public/index.html.gz
autonomous: false
requirements:
  - CFG-01

user_setup: []

must_haves:
  truths:
    - "server.sh contains a commented --mp3-bitrate example line"
    - "tools/public/index.html.gz is regenerated after all .svelte edits from Plans 02 and 03"
    - "The rebuilt ace-server binary embeds the updated WebUI (bitrate selector visible in browser)"
  artifacts:
    - path: "server.sh"
      provides: "commented --mp3-bitrate <N> example for operators"
      contains: "#.*--mp3-bitrate"
    - path: "tools/public/index.html.gz"
      provides: "Regenerated WebUI artifact containing the bitrate selector"
  key_links:
    - from: "tools/webui/src/components/RequestForm.svelte"
      to: "tools/public/index.html.gz"
      via: "./buildwebui.sh"
      pattern: "index.html.gz newer than RequestForm.svelte"
---

<objective>
Finalize the feature: add a commented `--mp3-bitrate` example to `server.sh` (CFG-01), regenerate the WebUI artifact with `./buildwebui.sh`, rebuild `ace-server` to embed the updated UI, and run a human checkpoint to confirm the selector appears and the full flow works end-to-end.

Purpose: CFG-01 ensures operators can discover and configure the server default; the build step closes the loop so the WebUI change is actually reachable in the browser.

Output: Updated `server.sh`, regenerated `tools/public/index.html.gz`, and a confirmed working end-to-end flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-mp3-bitrate-control/01-01-SUMMARY.md
@.planning/phases/01-mp3-bitrate-control/01-02-SUMMARY.md
@.planning/phases/01-mp3-bitrate-control/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add commented --mp3-bitrate example to server.sh</name>
  <files>server.sh</files>

  <read_first>
    - server.sh — read the full file (15 lines) to see the current flag list and where output-related flags belong
  </read_first>

  <action>
Edit server.sh to add a commented `--mp3-bitrate` example line.

Current server.sh ends with:
```bash
./build/ace-server \
    --host 0.0.0.0 \
    --port 8085 \
    --models ./models \
    --loras ./loras \
    --max-batch 1
```

Add the `--mp3-bitrate` example as a commented line in the output section. Insert it after `--max-batch 1` as a commented continuation line:

```bash
./build/ace-server \
    --host 0.0.0.0 \
    --port 8085 \
    --models ./models \
    --loras ./loras \
    --max-batch 1
    #--mp3-bitrate 128
```

Note: the line must be commented (starts with `#`) so it does not affect the default launch. The value 128 matches the server's hard-coded default so operators can see what the default is and adjust it. A brief inline comment explaining the purpose is welcome:

```bash
    #--mp3-bitrate 128    # MP3 encoding bitrate in kbps (valid: 32,40,48,56,64,80,96,112,128,160,192,224,256,320)
```
  </action>

  <verify>
    <automated>grep -n "mp3-bitrate" /Volumes/SN8100_2TB_A/Git/Code/acestep.cpp/server.sh</automated>
  </verify>

  <acceptance_criteria>
    - server.sh contains a line matching `#.*--mp3-bitrate`
    - The line is commented (starts with `#` or is preceded by `#` in the shell script context)
    - Running `bash -n server.sh` exits 0 (syntax check passes)
    - The active `./build/ace-server` invocation still starts correctly (the commented line is not parsed as an active argument)
  </acceptance_criteria>

  <done>server.sh includes a commented --mp3-bitrate 128 example with a comment describing valid values. Operators can discover and configure the default MP3 bitrate by uncommenting and changing this line.</done>
</task>

<task type="auto">
  <name>Task 2: Run buildwebui.sh to regenerate tools/public/index.html.gz</name>
  <files>tools/public/index.html.gz</files>

  <read_first>
    - buildwebui.sh — read the full file to understand the build command (typically `npm run build` inside tools/webui) and confirm there are no required env vars or prerequisites beyond npm
  </read_first>

  <action>
Run the WebUI build to regenerate the gzipped artifact:

```bash
cd /Volumes/SN8100_2TB_A/Git/Code/acestep.cpp && ./buildwebui.sh
```

This must complete without errors. The script recompiles the Svelte sources (including the updated RequestForm.svelte and state.svelte.ts from Plans 02 and 03) and writes the new `tools/public/index.html.gz`.

After the build, verify the artifact is newer than the source files:

```bash
ls -lt tools/public/index.html.gz tools/webui/src/components/RequestForm.svelte tools/webui/src/lib/state.svelte.ts tools/webui/src/lib/api.ts
```

`tools/public/index.html.gz` must appear at the top (most recently modified).
  </action>

  <verify>
    <automated>ls -lt /Volumes/SN8100_2TB_A/Git/Code/acestep.cpp/tools/public/index.html.gz /Volumes/SN8100_2TB_A/Git/Code/acestep.cpp/tools/webui/src/components/RequestForm.svelte | head -2</automated>
  </verify>

  <acceptance_criteria>
    - ./buildwebui.sh exits 0
    - tools/public/index.html.gz modification timestamp is newer than tools/webui/src/components/RequestForm.svelte
    - tools/public/index.html.gz modification timestamp is newer than tools/webui/src/lib/state.svelte.ts
    - The file size of tools/public/index.html.gz has changed from its pre-edit value (confirming it was actually rebuilt, not a no-op)
  </acceptance_criteria>

  <done>tools/public/index.html.gz regenerated from updated Svelte sources. The new artifact contains the bitrate selector. The C++ binary must now be rebuilt to embed this artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All four source areas modified:
- C++ (Plan 01): ServerFields.mp3_bitrate, parse_server_fields validation, synth_worker per-request bitrate, --mp3-bitrate startup guard
- TypeScript state (Plan 02): app.mp3Bitrate, localStorage persistence, synthSubmit/synthSubmitWithAudio updated
- Svelte UI (Plan 03): bitrate selector in format row, props-seeding $effect
- Config (this plan): server.sh commented example, tools/public/index.html.gz regenerated

You now need to rebuild the C++ binary and verify the full end-to-end flow.
  </what-built>

  <how-to-verify>
1. Rebuild the C++ binary to embed the updated index.html.gz:
   ```
   cmake --build build --config Release
   ```
   (or your local build command — e.g. `make -C build -j$(nproc)`)

2. Start the server with a non-default bitrate to test initialization:
   ```
   ./build/ace-server --host 0.0.0.0 --port 8085 --models ./models --loras ./loras --max-batch 1 --mp3-bitrate 192
   ```

3. Open http://localhost:8085 in your browser.

4. **BITUI-03**: Confirm the bitrate selector shows "192 kbps" as the selected value without you touching anything.

5. **BITUI-01**: Confirm the bitrate dropdown is visible and shows options: 96 / 128 / 160 / 192 / 256 / 320.

6. **BITUI-02**: Switch format to "WAV16" — confirm the bitrate dropdown disappears. Switch back to "MP3" — confirm it reappears.

7. **SRV-01 + SRV-03**: Select 320 kbps, submit a short synthesis request. After completion, download the MP3 and run:
   ```
   ffprobe -v quiet -print_format json -show_streams result.mp3 | grep bit_rate
   ```
   Must show ~320000 (320 kbps).

8. **SRV-02**: Test validation directly:
   ```
   curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:8085/synth \
     -H 'Content-Type: application/json' \
     -d '{"caption":"test","mp3_bitrate":200}'
   ```
   Must return `400`.

9. **SRV-03**: Test fallback (omit mp3_bitrate, server uses 192 default):
   ```
   curl -s -X POST http://localhost:8085/synth \
     -H 'Content-Type: application/json' \
     -d '{"caption":"test"}'
   ```
   Submit a short request and confirm the result encodes at 192 kbps (the --mp3-bitrate 192 server default).

10. **CFG-01**: Confirm `grep mp3-bitrate server.sh` shows a commented example line.
  </how-to-verify>

  <resume-signal>Type "approved" if all checks pass, or describe any failures so they can be addressed before the PR is opened.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buildwebui.sh output → C++ embed | The gzipped HTML artifact is embedded verbatim in the binary |
| server.sh → operator invocation | Commented example must not accidentally activate on uncomment without a value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | tools/public/index.html.gz stale artifact | mitigate | Verification step confirms index.html.gz mtime > source .svelte mtime. Human checkpoint includes visual confirmation that selector is present in browser. |
| T-04-02 | Tampering | server.sh: commented example becomes active on typo uncomment | accept | The `#--mp3-bitrate 128` form uses the hash comment character. If a user uncomments it, the value 128 is the server default — no harm. The comment includes valid-values list so operators know valid inputs. |
</threat_model>

<verification>
- `bash -n server.sh` exits 0
- `grep mp3-bitrate server.sh` shows commented line
- `ls -lt tools/public/index.html.gz tools/webui/src/components/RequestForm.svelte` — gz is newer
- Human checkpoint passed (bitrate selector visible, hides for WAV, seeds from props, 400 on invalid, 320 kbps actually encoded)
</verification>

<success_criteria>
- server.sh contains commented --mp3-bitrate 128 example with valid-values comment
- tools/public/index.html.gz regenerated after all .svelte edits
- C++ binary rebuilt with updated embedded WebUI
- Human-verified: selector visible for MP3, hidden for WAV, seeded from --mp3-bitrate flag, POST /synth with 320 encodes at 320 kbps, invalid value returns 400
</success_criteria>

<output>
After completion, create `.planning/phases/01-mp3-bitrate-control/01-04-SUMMARY.md`
</output>
